# Photos go here

Drag your photos into this folder (Add file ▸ Upload files), then use the
file name in **content.js** — e.g. a file called `nolanga-1.jpg` is written as
`"nolanga-1.jpg"` in content.js.

Tip: keep names simple and lowercase, no spaces (use dashes): `tayla-cover.jpg`.
